# Web UI Tab 介面架構說明

## 📋 概述

已將 Web UI 重構為基於 Tab 的多頁面架構，將上傳功能和資料庫檢視分離到不同的頁面中。

## 🏗️ 架構設計

### 檔案結構

```
templates/
├── base.html          # 基礎模板（包含 tab 導航）
├── upload.html        # 影片上傳頁面
├── database.html      # 資料庫檢視頁面
└── index.html         # 舊版頁面（已棄用，保留作為備份）
```

### 1. **基礎模板** (`base.html`)

**功能：**
- 提供統一的頁面框架
- 包含 tab 導航列
- 定義共用的樣式和腳本區塊

**Tab 導航：**
```html
📤 影片上傳     🗄️ 資料庫檢視
```

**區塊系統：**
- `{% block title %}` - 頁面標題
- `{% block extra_styles %}` - 額外樣式
- `{% block content %}` - 主要內容
- `{% block scripts %}` - JavaScript 腳本

### 2. **上傳頁面** (`upload.html`)

**路由：** `/`

**功能：**
- 影片檔案上傳
- 即時處理進度顯示
- 醫療數值結果展示
- 圖片模態視窗查看

**主要組件：**
1. 上傳表單區域
2. 處理狀態區域（進度條、狀態資訊）
3. 醫療數值列表
4. 圖片模態視窗

**JavaScript 功能：**
- 表單提交處理
- 狀態輪詢 (polling)
- 即時更新醫療數值
- 模態視窗控制

### 3. **資料庫檢視頁面** (`database.html`)

**路由：** `/database`

**功能：**
- 顯示所有 MongoDB 中的影片記錄
- 連接狀態監控
- 查看詳細醫療數值
- 精美的卡片式展示

**主要組件：**
1. 資料庫連接狀態標記
2. 重新載入按鈕
3. 影片記錄卡片網格
4. 醫療數值詳情區域
5. 圖片模態視窗

**影片卡片資訊：**
- 影片名稱
- Session ID
- 處理狀態（已完成/處理中）
- 檔案路徑
- 處理時間
- 總幀數
- 查看詳情按鈕

**JavaScript 功能：**
- 自動載入資料庫內容
- MongoDB 連接狀態檢查
- 影片記錄動態渲染
- 醫療數值詳情查看

## 🎨 設計特點

### Tab 導航樣式
- **未選中狀態：** 灰色文字，透明背景
- **Hover 狀態：** 淺藍色背景，藍色文字
- **選中狀態：** 白色背景，藍色文字，底部藍色線條

### 響應式設計
- 桌面版：雙欄佈局
- 平板/手機：單欄佈局
- Tab 導航在小螢幕上垂直排列

### 配色方案
- **主色調：** 藍色漸層 (#4facfe → #00f2fe)
- **背景：** 紫色漸層 (#667eea → #764ba2)
- **狀態顏色：**
  - 成功：綠色 (#28a745)
  - 處理中：黃色 (#ffc107)
  - 錯誤：紅色 (#dc3545)

## 🔗 路由結構

### Flask 路由

| 路由 | 模板 | 功能 |
|------|------|------|
| `/` | `upload.html` | 影片上傳頁面 |
| `/database` | `database.html` | 資料庫檢視頁面 |
| `/upload/` | - | API：處理影片上傳 |
| `/status/` | - | API：取得處理狀態 |
| `/stop/` | - | API：停止處理 |
| `/api/mongodb/status` | - | API：MongoDB 連接狀態 |
| `/api/mongodb/videos` | - | API：取得影片列表 |
| `/api/mongodb/medical_values/<session_id>` | - | API：取得醫療數值 |
| `/analysis/<path>` | - | 靜態檔案：分析結果圖片 |

## 🚀 使用指南

### 啟動步驟

1. **重啟 Flask 應用程式**（必須）：
```bash
# 停止現有 Flask
pkill -f "python.*web_app.py"

# 啟動新版本
python web_app.py
```

2. **訪問應用程式**：
```
http://localhost:3001
```

### 使用流程

#### 📤 上傳影片流程
1. 在首頁（`/`）選擇影片檔案
2. 點擊「開始處理」
3. 查看即時處理進度
4. 處理完成後查看醫療數值列表
5. 點擊卡片查看詳細圖片

#### 🗄️ 查看資料庫流程
1. 點擊「資料庫檢視」tab
2. 自動載入所有影片記錄
3. 查看每個影片的基本資訊
4. 點擊「查看醫療數值詳情」按鈕
5. 查看該影片的所有醫療數值
6. 點擊醫療數值卡片查看完整圖片

## 📊 資料流程

### 上傳頁面資料流
```
用戶上傳影片
    ↓
POST /upload/
    ↓
背景處理開始
    ↓
前端開始輪詢 /status/
    ↓
即時更新進度和結果
    ↓
處理完成，儲存到 MongoDB
```

### 資料庫頁面資料流
```
頁面載入
    ↓
GET /api/mongodb/status （檢查連接）
    ↓
GET /api/mongodb/videos （取得影片列表）
    ↓
顯示影片卡片
    ↓
用戶點擊「查看詳情」
    ↓
GET /api/mongodb/medical_values/{session_id}
    ↓
顯示醫療數值詳情
```

## 🎯 主要改進

### 相較於舊版 UI

1. **更清晰的功能分離**
   - 上傳和查看功能獨立
   - 避免頁面過於擁擠

2. **更好的用戶體驗**
   - Tab 導航直觀易用
   - 卡片式設計美觀清晰
   - 響應式佈局適配各種螢幕

3. **更易維護**
   - 模板繼承減少重複代碼
   - 功能模組化
   - 樣式統一管理

4. **更強大的功能**
   - 即時查看所有歷史記錄
   - MongoDB 連接狀態監控
   - 更詳細的影片資訊展示

## 🔧 技術細節

### Jinja2 模板繼承
```python
{% extends "base.html" %}  # 繼承基礎模板
{% block content %}        # 覆寫內容區塊
    <!-- 頁面特定內容 -->
{% endblock %}
```

### 動態 Tab 高亮
```python
<a href="/" class="tab-button {% if request.path == '/' %}active{% endif %}">
```

### API 資料格式
```json
{
    "success": true,
    "data": [...],
    "count": 4
}
```

## 📝 注意事項

1. **必須重啟 Flask** 才能使用新的路由
2. **圖片路徑** 已修正為通過 `/analysis/` 路由
3. **舊版 index.html** 保留作為備份，可以刪除
4. **MongoDB 必須運行** 才能使用資料庫檢視功能

## 🐛 疑難排解

### 問題：Tab 導航無法切換
**原因：** Flask 未重啟  
**解決：** 重新啟動 Flask 應用程式

### 問題：資料庫頁面顯示「未連接」
**原因：** MongoDB 服務未運行  
**解決：**
```bash
python -c "from mongo_manager import start_local_mongodb; start_local_mongodb()"
```

### 問題：圖片無法顯示
**原因：** 圖片路徑錯誤  
**解決：** 確認已使用最新版本的 `web_app.py`（已修正路徑轉換邏輯）

## 🔮 未來改進方向

- [ ] 新增搜尋和過濾功能
- [ ] 新增資料匯出功能（CSV/Excel）
- [ ] 新增批量操作（批量刪除、批量下載）
- [ ] 新增資料統計圖表（趨勢圖、分佈圖）
- [ ] 新增用戶設定頁面
- [ ] 新增深色模式
- [ ] 新增多語言支援

## 📚 相關文件

- [`MONGODB_VIEW_FEATURE.md`](MONGODB_VIEW_FEATURE.md) - MongoDB 功能說明
- [`README.md`](README.md) - 專案總體說明