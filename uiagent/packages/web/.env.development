# Development environment variables for Vite
#
# ===================================================================
# 重要：選擇正確的配置模式
# ===================================================================
#
# 模式 1: 本地開發 (推薦用於本機測試)
# -------------------------------------
# 啟用此模式：註解掉下面的 VITE_BASE_PATH 和 VITE_API_URL
# 這會使用相對路徑，適合直接訪問 http://localhost:4000
#
VITE_BASE_PATH=
# 本地開發時，API 請求走 Vite 的 proxy 配置
VITE_API_URL=/api/generate
#
# 模式 2: Kubeflow/JupyterHub Proxy 環境（生產模式）
# -------------------------------------
# 啟用此模式：設置完整的 proxy 路徑
# 例如:
# VITE_BASE_PATH=/notebook/d000018238/a2/proxy/4000/
# VITE_API_URL=http://localhost:4001/api/generate
#
# 或使用相對 URL（推薦，基於當前頁面路徑）:
# VITE_API_URL=../4001/api/generate
#
# 注意：
# 1. 路徑必須以 / 開頭和結尾
# 2. 路徑要與實際的 proxy URL 匹配
# 3. 修改後必須重新構建 (pnpm build)
#
#VITE_BASE_PATH=/notebook/d000018238/a2/proxy/4000/
#VITE_API_URL=../4001/api/generate
#
# ===================================================================
# API URL 配置說明
# ===================================================================
#
# VITE_API_URL 控制前端如何調用 API：
#
# 開發模式：
#   VITE_API_URL=/api/generate (走 Vite proxy)
#
# 生產模式（同 domain 不同 port）：
#   VITE_API_URL=../4001/api/generate (相對路徑，往上一層再進 4001)
#
# 生產模式（完整 URL）：
#   VITE_API_URL=http://localhost:4001/api/generate
#
# ===================================================================
# Proxy 環境的已知限制
# ===================================================================
#
# Vite 開發模式在 proxy 環境下可能遇到的問題：
# 1. HMR (熱模組替換) WebSocket 連接問題
# 2. 動態注入的虛擬模組 (@vite/client, @react-refresh) 路徑問題
# 3. 絕對路徑 vs 相對路徑的衝突
#
# 解決方案：
# - 選項 A: 使用生產構建 (pnpm build) 而非開發模式 ⭐ 推薦
# - 選項 B: 配置正確的 VITE_BASE_PATH 並使用 --host 0.0.0.0
# - 選項 C: 使用 SSH tunnel 直接訪問 localhost:4000
#
# ===================================================================
