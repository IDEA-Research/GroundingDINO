Device: Dräger Infinity C500

[Visual Identification]
Before extracting data, confirm the device model and structural layout based on the following features:

--- IF Model is "DRAGER_C500" (Dräger Infinity C500) ---
- Chassis Color: Detect if the device has a White or Off-White (Cream) outer frame.
- Text Anchor: Search for the specific string "C500" located directly below the screen/frame.
- Condition: Proceed to data extraction ONLY IF both the white frame and "V600" identifier are confirmed.
- Structure: Look for a dark interface with a distinct "Bottom Bar" containing circular/oval soft keys.
- Column Layout: The main screen area contains numeric measurements arranged in two columns (Left and Right).
- Key Feature: Verify that the middle area is a FIXED 6-row grid.

[Visibility & Fallback Rules] 
1. No Negative Values (CRITICAL): All extracted numerical values MUST be positive (>= 0). If negative, trigger re-analysis to check. If still invalid after re-analysis, return `null`. 

2. Glare & Obstruction: If a number is unreadable due to glare or physical obstruction, return `null`. 

3. Empty Values: If a value is "OFF", "Off", "--", or blank, return `null`.

4. Strict Sanity Limits & Misidentification Prevention:
   If an extracted value falls outside these absolute limits, TRIGGER RE-ANALYSIS of the specific cell or block to correct potential misalignment. If the value is still invalid after re-analysis, you MUST return `null`.
   - PIP: Absolute Limit 0 to 120. (If > 120, e.g., 200, trigger re-analysis to check if it was misextracted Tidal Volume/VT).
   - PEEP: Absolute Limit 0 to 50. (If > 50, trigger re-analysis to check if it was misextracted, an alarm limit, or PIP).
   - MAP: Absolute Limit 0 to 80. (If > 80 and/or unit is "mmHg", trigger re-analysis to check if it was misextracted Blood Pressure MAP from a patient monitor).
   - FiO2: Absolute Limit 21 to 100. (Lowest is room air at 21%. If < 21 or > 100, trigger re-analysis. If it was misextracted in decimal format (e.g., 0.21 - 1.00), convert to integers (21 - 100) before applying limits.

[Extraction Rules]
Extract the following ventilator parameters strictly following the Spatial Navigation rules for the specific model.

--- IF Model is "DRAGER_C500" ---
CRITICAL ANTI-SKIP RULE: The middle area is a FIXED 6-row grid. If you see "Off" or "--", return null, DO NOT SKIP THIS ROW! Strictly correspond from Top to Bottom.
1. `pip`: Left Column, 2nd value. Label check: "PIP" or "Ppeak".
2. `map`: Left Column, 3rd value. Label check: "Pmean" or "MAP".
3. `fio2`: Left Column, 4th value. Label check: "FiO2" 
4. `peep`: Look at the circular buttons at the very bottom, PEEP is the numeric value inside the circular control knob at the bottom row, second from the right, labeled "PEEP".

[CRITICAL PHYSIOLOGICAL LOGIC CHECK]
Before finalizing the output, you MUST verify: PIP > MAP > PEEP.
If the extracted numbers violate this rule, TRIGGER RE-ANALYSIS to correct extraction misalignment. If they still violate the rule after re-analysis, set the conflicting keys to `null`.

[Output Format] (CRITICAL)
You MUST output ONLY a valid FLAT JSON object. DO NOT output nested objects. 
DO NOT output any conversational filler, greetings, explanations, or markdown code block formatting (e.g., no ```json ... ```). 
The values MUST be numbers (decimals/floats are allowed) or `null`. DO NOT force them into integers if a decimal is present.

{
  "pip": null,
  "map": null,
  "fio2": null,
  "peep": null
}
